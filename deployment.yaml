apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: blueocean-demo
spec:
  template:
    metadata:
      labels:
        app: blueocean-demo
    spec:
      containers:
      - name: blueocean-demo
        image: marceloquadros/blueocean-demo:1.0.0-SNAPSHOT
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
